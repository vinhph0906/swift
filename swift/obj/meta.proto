syntax = "proto3";

package meta;

// Generate module with: protoc -I. --python_out=. meta.proto

message Attr {
    string key = 1;
    string value = 2;
}

message Metadata {
    repeated Attr attrs = 1;
}
